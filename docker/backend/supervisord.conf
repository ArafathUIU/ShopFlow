worker:
  image: shopflow-backend
  command: php /var/www/html/artisan queue:work --sleep=3 --tries=3 --max-time=3600
  volumes:
    - ./backend/storage:/var/www/html/storage
  environment:
    - APP_ENV=production
  depends_on:
    - postgres
    - redis
  restart: unless-stopped
